# ─────────────────────────────────────────────────────────────
# Flamingo CocoBeach UAT – Render Blueprint
# ─────────────────────────────────────────────────────────────
# Project: Flamingo CocoBeach UAT
# Bucket: same S3 endpoint as Ilot CocoBeach, container = "flamingo"
# ─────────────────────────────────────────────────────────────

databases:
  - name: flamingo-db
    plan: free
    databaseName: flamingo
    user: flamingo

services:
  # ─── Backend API ────────────────────────────────────────
  - type: web
    name: flamingo-api
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: rm -f package-lock.json && npm install --include=dev && npx prisma generate && npx prisma db push --accept-data-loss && node prisma/seed.js
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: flamingo-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: https://flamingo-client.onrender.com,https://flamingo-admin.onrender.com
      - key: S3_ENDPOINT
        sync: false
      - key: S3_ACCESS_KEY
        sync: false
      - key: S3_SECRET_KEY
        sync: false
      - key: S3_BUCKET
        value: flamingo
      - key: S3_PUBLIC_URL
        sync: false
      - key: NODE_ENV
        value: production
      - key: REQUEST_TIMEOUT_MS
        value: "600000"
      - key: CONNECTION_TIMEOUT_MS
        value: "600000"
      - key: NODE_VERSION
        value: "20.19.0"
      - key: BREVO_API_KEY
        sync: false
      - key: BREVO_SENDER_EMAIL
        value: noreply@flamingo-cocobeach.com
      - key: ADMIN_URL
        value: https://flamingo-admin.onrender.com

  # ─── Client (public website) ────────────────────────────
  - type: web
    name: flamingo-client
    runtime: static
    rootDir: client
    buildCommand: rm -f package-lock.json && npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /api/*
        destination: https://flamingo-api.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: "20.19.0"

  # ─── Admin panel ────────────────────────────────────────
  - type: web
    name: flamingo-admin
    runtime: static
    rootDir: admin
    buildCommand: rm -f package-lock.json && npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /api/*
        destination: https://flamingo-api.onrender.com/api/*
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: "20.19.0"
